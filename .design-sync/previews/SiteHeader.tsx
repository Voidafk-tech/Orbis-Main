import React from 'react';
import { SiteHeader } from 'orbis-accounting-landing-page';
import './shell.css';

/**
 * The header as App.tsx mounts it — full bleed, with the lockup, nav,
 * language toggle, phone number and primary CTA in one row.
 */
export const Default = () => <SiteHeader />;
