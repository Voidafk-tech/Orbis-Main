import React from 'react';
import { WhyOrbis } from 'orbis-accounting-landing-page';
import './shell.css';

/** The differentiator rows on `.sec--paper`, bulleted with SatelliteBullet. */
export const Default = () => (
  <main>
    <WhyOrbis />
  </main>
);
