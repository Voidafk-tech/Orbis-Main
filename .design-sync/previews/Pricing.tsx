import React from 'react';
import { Pricing } from 'orbis-accounting-landing-page';
import './shell.css';

/**
 * The three plan tiers on `.sec--paper`, with the middle tier featured and
 * each tier's inclusions listed beneath it.
 */
export const Default = () => (
  <main>
    <Pricing />
  </main>
);
