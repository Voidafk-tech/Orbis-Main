import React from 'react';
import { Steps } from 'orbis-accounting-landing-page';
import './shell.css';

/** The three-step process band, ruled off from the section above it. */
export const Default = () => (
  <main>
    <Steps />
  </main>
);
