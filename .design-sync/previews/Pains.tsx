import React from 'react';
import { Pains } from 'orbis-accounting-landing-page';
import './shell.css';

/** The four-column "already living with" block, on the dark ground. */
export const Default = () => (
  <main>
    <Pains />
  </main>
);
